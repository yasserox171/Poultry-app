name: poultry_app
description: تطبيق دواجني - إدارة مشاريع الدواجن
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  percent_indicator: ^4.2.3
  flutter_animate: ^4.5.0
  animated_text_kit: ^4.2.2
  provider: ^6.1.2
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  intl: ^0.19.0
  curved_navigation_bar: ^1.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
